import 'package:flutter/material.dart';

class LugaresPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Scaffold(
        body: Center(
          child: Text('Lugares donde comprar'),
        ),
      ),
    );
  }
}
